# Managed by dsh-claude-code. Do not edit; copy this preset under a new id to customize it.
name: Claude Code CLI
description: Use the local Claude Code CLI as the complete agent runtime inside DSH.
order: 10
